[package]
name = "degenbot_contract_bindings"
version = "0.1.0"
edition = "2021"

[dependencies]
alloy = { version = "2.0.5", default-features = false, features = ["contract", "sol-types"] }
serde = { version = "1.0", features = ["derive"] }